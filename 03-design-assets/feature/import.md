# 导入功能点设计模板

## 适用场景
- 用于批量导入、模板下载、文件上传、导入进度反馈、冲突处理、错误数据在线修正、导入结果查看等需求。
- 适用于用户、设备、资产、策略、对象组、标签、任务配置等具备批量数据录入诉求的页面。
- 当导入流程只包含简单上传和结果反馈时，可使用配置弹窗与结果弹窗承载；当存在校验失败记录且需要用户修正时，应提供错误列表在线编辑和再次提交能力。

## 推荐页面组合
| 页面ID | 页面名称 | 页面类型 | 页面用途 | 入口方式 | 关键交互 |
|---|---|---|---|---|---|
| import-config | 批量导入配置 | 弹窗表单页 | 下载模板、上传数据并设置冲突处理规则 | 来源列表页点击“批量导入” | 下载模板、上传文件、选择冲突处理规则、开始导入 |
| import-progress | 正在导入 | 进度弹窗 | 展示文件导入进度 | import-config 点击“导入” | 展示文件名、进度条和百分比，完成后按结果分流 |
| import-success | 导入成功 | 结果弹窗 | 展示完全成功或仅存在冲突跳过记录的结果 | import-progress 导入完成 | 展示成功数和跳过数，支持下载冲突报告和关闭 |
| import-error-list | 导入异常处理 | 弹窗列表页 | 查看、修正或删除校验失败的数据行 | import-progress 存在失败记录 | 在线编辑错误字段、删除记录、下载错误报告、提交修正数据 |
| import-partial-result | 导入完成（部分失败） | 结果弹窗 | 汇总成功、失败与冲突跳过数量，并提供后续处理 | import-progress 导入完成且仍存在失败记录 | 下载报告、重新导入、关闭 |

## 推荐流程
```text
来源列表页
  批量导入配置
    正在导入
      导入成功
      导入异常处理
        导入成功
        导入完成（部分失败）
      导入完成（部分失败）
```

## 页面结构

- 页面类型：纵向步骤条弹窗表单页
- 页面类型 ID：`page-form-modal-stepper`

```text
┌──────────────────────────────────────────────┐
│ 批量导入配置                         [关闭]   │
├──────────────────────────────────────────────┤
│                                              │
│  ① 下载模板                                  │
│  │  还没有导入模板？先点击下载并填写对应的信息 │
│  │  [下载模板]                               │
│  │                                           │
│  ② 上传模板                                  │
│  │  将您填好信息的文件上传于此，支持.xls/.xlsx │
│  │  不可超过1MB                              │
│  │  [上传模板]  文件名.xlsx   50%   [移除]    │
│  │                                           │
│  ③ 导入规则                                  │
│     当导入数据和已有数据存在冲突时：          │
│     (•) 跳过   ( ) 替换                      │
│                                              │
│                               [确定][取消]    │
└──────────────────────────────────────────────┘
```

#### 组件信息

- 弹窗表单页：使用 `IxProFormModal`。
- 步骤条：使用 `IxStepper` 承载纵向步骤条（下载模板、上传模板、导入规则）；本页面模式明确要求纵向步骤条，属于组件映射中“特殊说明使用纵向步骤条”的场景，配置 `IxStepper` 的 `vertical` 属性使步骤条沿竖直方向排列（步骤条默认横向，纵向仅在本模式明确要求时使用）；如当前项目已有分步表单封装，优先使用 `IxProFormStepper`。
- 表单：步骤内容使用 `IxProForm`。
- 弹窗：仅非表单内容使用 `IxModal`，表单流程不降级为 `IxModal` 手工组合。

## 页面设计要点

### 1. 下载模板/上传文件
- 页面名称：批量导入配置。
- 页面ID：`import-config`。
- 页面类型：弹窗表单页。
- 页面用途：用户通过下载并填写 Excel 模板，批量导入业务数据；可指定导入数据与已有数据冲突时的处理方式。
- 页面入口：来源列表页点击“批量导入”入口。

#### 页面内容
- 导入步骤区以纵向步骤条展示三个阶段：下载模板、上传模板、导入规则；步骤与步骤之间使用纵向连线表达流程关系，步骤标题下方不使用横向分割线。
- 下载模板：
  - 提示文案：“还没有导入模板？先点击下载并填写对应的信息”。
  - “下载模板”按钮用于获取导入 Excel 模板。
- 上传模板：
  - 提示文案：“将您填好信息的文件上传于此，支持.xls、.xlsx格式，不可超过1MB”。
  - “上传模板”按钮用于选择本地文件。
  - 上传完成后展示文件名、上传进度，以及移除文件的关闭入口。
- 导入规则：
  - 提示文案：“当导入数据和已有数据存在冲突时：”。
  - 单选项“跳过”：默认选中，保留已有数据，不导入冲突记录。
  - 单选项“替换”：以导入文件中的记录覆盖已有冲突数据。
- 底部操作：
  - “确定”：校验文件后启动导入任务。
  - “取消”：关闭弹窗并放弃本次配置。
  - 关闭图标：关闭弹窗。

#### 关键交互
- 用户应先下载模板、按字段规范填写后再上传。
- 上传文件仅允许 `.xls`、`.xlsx` 格式，大小不得超过 1MB。
- 文件上传完成后，需要展示文件名称、上传进度和移除入口，便于用户确认或重新选择文件。
- 点击“确定”后进入 `import-progress`。
- 冲突记录是否跳过或替换，取决于用户在导入规则中选择的处理方式。

### 2. 上传中
- 页面名称：正在导入。
- 页面ID：`import-progress`。
- 页面类型：进度弹窗。
- 页面用途：在后台解析并写入上传文件时，向用户反馈当前处理的文件及实时进度。
- 页面入口：批量导入配置页点击“确定”。

#### 页面内容
- 标题展示“正在导入”。
- 展示 Excel 文件图标。
- 展示当前导入的文件名称。
- 展示进度条与百分比，例如 `50%`。

#### 关键交互
- 导入期间，背景中的配置弹窗保持不可操作状态，避免用户重复提交或修改导入配置。
- 进度完成后，根据导入结果进入不同状态：
  - 全部记录成功，或失败记录为零、仅存在冲突跳过记录时，进入 `import-success`。
  - 存在字段校验失败记录且需要人工修正时，进入 `import-error-list`。
  - 导入完成后仍存在失败记录且不再进入在线修正流程时，进入 `import-partial-result`。

### 3. 错误信息在线编辑后提交
- 页面名称：导入异常处理。
- 页面ID：`import-error-list`。
- 页面类型：弹窗列表页。
- 页面用途：展示导入校验失败的数据，支持用户在线修正错误字段、删除无效数据后再次提交。
- 页面入口：正在导入完成后，存在字段校验失败记录。

#### 页面内容
- 导入结果摘要：
  - 展示警告图标和标题“导入完毕”。
  - 汇总成功导入数量、失败数量、因与原信息冲突而跳过的数量。
  - 失败数字使用醒目提示。
  - 提供“下载错误报告”按钮，用于下载失败记录及错误原因。
- 错误列表：
  - 标题为“错误列表”。
  - 通过表格展示失败的导入数据。
  - 可展示字段包括姓名、性别、工号、手机号、邮箱、工作地、直属上级工号和操作。
  - 性别可通过下拉选择修正。
  - 工号、手机号、邮箱、直属上级工号可直接编辑。
  - 操作列提供“删除”，用于从本次待提交数据中移除该记录。
  - 存在校验异常的输入框以错误状态标识，用户需要修正字段内容后重新提交。
  - 表格右上角提供刷新入口，用于重新加载或恢复错误数据列表。
- 底部操作：
  - “提交”：提交修正后的失败记录。
  - “取消”：放弃本次错误修正并关闭弹窗。
  - 关闭图标：关闭弹窗。

#### 关键交互
- 导入后端需对每行进行字段格式、必填项及业务关联关系校验。
- 用户可直接修改存在错误提示的字段；修改后应重新校验。
- 用户也可删除无法修正或无需导入的错误行。
- 删除记录前需弹出确认提示：“确定要删除这条数据吗？”。
  - 点击“确定”：删除该条错误记录。
  - 点击“取消”：保留该记录。
- 点击“提交”后，仅提交修正后保留的失败记录，不重复处理已成功或已跳过的数据。
- 再次提交完成后，根据结果进入：
  - `import-success`：修正后已无失败记录。
  - `import-partial-result`：仍存在未解决失败记录。

## 结果状态补充

### 导入成功
- 页面ID：`import-success`。
- 展示成功图标与标题“导入成功”。
- 结果文案包含成功导入的数据条数，以及因与原有信息冲突、按“跳过”规则未导入的数据条数。
- 支持“下载冲突报告”，报告应包含冲突记录及其冲突原因或冲突字段。
- 支持“关闭”和关闭图标，关闭后返回来源列表页。

### 导入完成（部分失败）
- 页面ID：`import-partial-result`。
- 展示警告图标与标题“导入完毕”。
- 结果文案包含成功导入的数据条数、失败数据条数、因与原信息冲突跳过的数据条数。
- 失败数字使用醒目提示。
- 支持“下载冲突报告”或未成功处理数据报告；实际报告范围需明确是否同时包含失败与冲突跳过记录。
- 支持“重新导入”，返回 `import-config`，允许重新下载模板、修改数据文件并再次上传。
- 存在未解决失败记录时，不应展示为完全成功状态。
- 重新导入应开启新的导入任务，不应重复写入本次已成功导入的数据。

## Coding提示
- 推荐复用组件：弹窗表单、纵向步骤、文件上传、单选规则、进度弹窗、结果反馈、可编辑表格、表单校验、二次确认。
- Mock数据要求：覆盖上传前、上传完成、导入中、全部成功、仅冲突跳过、存在失败记录、在线修正后成功、在线修正后仍部分失败、删除错误行等状态。
- 校验规则：覆盖文件格式、文件大小、必填项、字段格式、业务关联关系、冲突记录处理规则。
- 状态联动：导入成功后刷新来源列表页；提交修正数据时只处理保留的失败记录，不重复写入已成功或已跳过数据。
- 极端情况：重复点击导入、导入过程中关闭、文件上传后移除、错误列表为空、全部错误行被删除、报告下载失败、网络异常、模板版本不匹配。
