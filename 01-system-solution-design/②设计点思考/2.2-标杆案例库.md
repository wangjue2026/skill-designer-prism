# 织雨设计智脑：B 端需求类型全景图与标杆案例库（门户）

> 💡 **中枢定位**：
> 本文档是织雨在执行 **【方案系统设计主线】** 步骤 2（设计点思考）时的**唯一入口与定性中枢**。
>
> 📚 **文档结构说明（重要）**：
> 本文档**只承载【需求类型全景图】与【分册索引】**，不放具体案例内容。
> 各需求类型的完整兵法（通用设计场景清单 + 标杆案例深度蒸馏 + 复用决策树）已**按类型下沉为独立分册**，统一存放于 [`标杆案例库/`](./标杆案例库/) 子目录。
> 👍 这样做的好处：**新增蒸馏只需读写一个类型分册**，不必重读上千行的总库，维护成本大幅下降。
>
> 🔗 **与步骤 1、步骤 4 的上下游咬合逻辑**：
> 1. **步骤 1 完成时（需求理解与分析）**：在输出痛点与体验目标的同时，**对照本文档【全景图】，完成【需求类型定性】**；
> 2. **步骤 2 启动时（设计点思考）**：**必须强制进入该类型分册**：
>    - 先对照分册的【板块一：通用设计场景清单】，检查本次业务是否遗漏关键子场景（如连通性校验、影子资产认领、批量防呆）；
>    - 再对照分册的【板块二：标杆案例深度蒸馏】，学习已沉淀的标杆案例（当前仅 Type-07 有：XDR 检测任务）的落地手法与“为什么这样设计（Why）”；
>    - 比对本次业务与标杆案例的异同，完成经验复用，输出本次的《设计发力点与拟解决方案》；
> 3. **步骤 4 落地时（布局与结构）**：根据选定的方案架构，无缝映射到 `03-design-assets/` 组件库进行装配。

---

## 🗺️ 一、前置：B 端常见需求类型全景图与定性自检

当规划人员输入原始需求并完成步骤 1 的本质理解后，通过下表快速完成**需求类型定性**：

| 需求类型编号 | 需求类型名称 | 业务本质（解决什么核心问题） | 常见的真实业务需求场景举例 | 对应标杆案例 |
| :---: | :--- | :--- | :--- | :--- |
| **Type-01** | **对象与资产管理类** | 掌握海量物理、虚拟或数字实体的全貌台账、实时运行状态、权属责任与拓扑归属，实现高效管控、批量维护与应急排障 | • **终端安全管控**（如 SASE 办公 PC、移动终端准入合规体检与阻断）<br>• **主机与算力资产**（如数据中心服务器、云主机 ECS、虚拟机台账）<br>• **云原生容器管理**（如 Kubernetes 集群、节点 Node、Pod 实例清单）<br>• **账号与身份权限**（如企业员工目录、IAM 用户、角色与特权账号 PAM）<br>• **数据与数字资产**（如对外 API 接口清单、数据库敏感字段分类分级台账、域名与 SSL 证书生命周期）<br>• **网络与基础设施**（如交换机、路由器、防火墙网关设备及端口台账） | ⏳ 待蒸馏（暂无案例） |
| **Type-02** | **配置对接与数据同步类** | 跨异构系统、跨网络隔离区或跨平台打通控制流与数据流，确保凭据安全互通、字段精确映射对齐与数据无损增量同步 | • **异构数据库同步**（如 MySQL/Oracle 到 ClickHouse/ES 的全量与实时增量 CDC 管道）<br>• **第三方平台集成**（如企业微信/钉钉通讯录同步、飞书多维表格接入、OAuth2/SAML 单点登录认证对接）<br>• **网络对等互联打通**（如混合云跨 VPC 对等连接 Peering、专线 Direct Connect、云企业网 CEN 实例绑定）<br>• **外部通道与日志推送**（如 Syslog 日志外发、Kafka 消息流转接入、Webhook 告警回调通道配置）<br>• **开放平台应用接入**（如开发者 API 凭证管理、服务回调地址与签名机制配置） | ⏳ 待蒸馏（暂无案例） |
| **Type-03** | **监控度量与驾驶舱类** | 汇聚海量微观时序数据与指标状态，提供宏观秒级业务感知、异常尖刺预警与多维趋势研判，辅助全局运营与架构决策 | • **核心业务 SLA 态势大盘**（核心接口调用成功率、P99 响应耗时、系统吞吐 TPS 实时监控仪表盘）<br>• **基础设施资源与成本看板**（各业务线 CPU/内存资源水位、云存储配额消耗、按月账单走势驾驶舱）<br>• **网络质量全景监测**（全国/全球网络链路时延热力图、丢包率曲线、专线峰值带宽与拥塞监控）<br>• **企业安全综合态势**（全网安全威胁等级、拦截恶意攻击总量、高危未修复资产分布大屏） | ⏳ 待蒸馏（暂无案例） |
| **Type-04** | **链路排障与拓扑分析类** | 在复杂的网状交织与层层调用的系统架构中，穿透黑盒依赖，顺藤摸瓜秒级定位性能瓶颈与级联故障因果源头 | • **分布式微服务调用链排障**（从用户请求 ➔ API 网关 ➔ 业务服务 ➔ Redis/MySQL 的调用全景拓扑与慢查询定界）<br>• **物理/虚拟网络拓扑可观测**（跨地域 VPC、VPN 网关、Transit Gateway 之间的网络路由拓扑图）<br>• **业务上下游依赖与影响面分析**（核心服务受损时，秒级推演上下游受波及的 20+ 个业务模块与受损半径）<br>• **数据血缘与流转关系图谱**（追踪某张核心数据报表从原始日志经数仓多层清洗流转的全生命周期路径） | ⏳ 待蒸馏（暂无案例） |
| **Type-05** | **事件研判与调查处置类** | 聚合分散且海量的离散告警与异常信号，按时序串联因果证据链，消除告警风暴，驱动跨角色协同处置与闭环自愈 | • **XDR 安全事件调查研判**（还原黑客钓鱼入侵 ➔ 横向渗透 ➔ 提权 ➔ 数据渗漏全过程证据链）<br>• **智能告警中心与事故协同**（将 10,000+ 条网络抖动告警聚类为 1 起关键事故，指派应急小组并联动自愈脚本）<br>• **高危合规审批与流转工单**（涉密数据外发审批、特权操作申请、关键资产下线阻断审核工单）<br>• **故障复盘与事故复原**（事发时段指标与操作行为时序回放、责任归因与改进项跟踪） | ⏳ 待蒸馏（暂无案例） |
| **Type-06** | **策略规则与编排类** | 承载高复杂度、多分支条件判定与严格配额防呆的业务控制逻辑，确保规则不冲突、执行不越界、发布可预期 | • **Web 应用防火墙 (WAF) 策略编排**（按 URI 路径配置 IP 黑白名单、CC 频次防御、SQL/XSS 拦截规则）<br>• **API 网关流量治理策略**（灰度金丝雀权重切流规则、熔断降级阈值、CORS 跨域与访问黑名单）<br>• **零信任访问控制 (ZTNA) 策略**（根据角色身份、所在物理位置、终端安全基线动态判定访问权限）<br>• **数据安全脱敏与合规策略**（身份证/手机号/银行卡等敏感字段的掩码脱敏与导出限制规则编排） | ⏳ 待蒸馏（暂无案例） |
| **Type-07** | **检测任务与批量巡检类** | 以“任务”为主键，把分散的检测/扫描/巡检能力编排成一次可周期调度、可复用、可追溯多轮历史的作业，并通过任务清单持续暴露执行态与失败面 | • **漏洞检测与扫描任务**（XDR 检测任务、漏洞扫描器扫描任务、弱口令扫描任务）<br>• **资产体检与合规巡检**（主机安全体检、等保合规巡检、配置基线核查任务）<br>• **数据质量与血缘巡检**（数仓表新鲜度/空值/波动巡检任务）<br>• **批量运维作业**（批量备份、批量下发、批量采集任务）<br>• **周期性情报与信誉刷新**（威胁情报库同步、证书到期巡检任务） | ✅ XDR 检测任务（已蒸馏） |

---

## 🔍 二、类型识别判据与边界（定性铁律 · 定性时必读）

> 本节是**需求类型定性**的集中判据场。分册是"已确定类型之后"才去查的经验资产，**判据则统一收敛在此**，避免 7 个分册各写一份、口径不一。

### 2.1 定性顺序（三步走）

1. **先问「主键是什么」** —— 它管的是哪一类对象？
   `实体(01)` / `打通关系(02)` / `指标观测(03)` / `依赖链路(04)` / `离散事件(05)` / `长期规则(06)` / `执行作业(07)`
2. **再问「有没有执行实例与结果产物」** —— 有则偏向 Type-07；无且是持续观测则偏向 Type-03。
3. **最后用成对辨析确认**（见 2.3），排除误判。

### 2.2 各类型识别判据（须**同时满足**才可判定）

| 类型 | 判定主键 | 须同时满足的识别特征 |
| :---: | :--- | :--- |
| **Type-01** | 实体 | ① 对象长期存在，有自己的台账属性（权属 / 归属 / 状态）② 需按组织或分组维度批量维护 ③ 有生命周期（接入 ➔ 变更 ➔ 下线） |
| **Type-02** | 打通关系 | ① 跨异构系统或跨网络隔离区 ② **双向**（控制流 + 数据流）③ 需凭据鉴权、字段映射与冲突消解 ④ 关注数据一致性 |
| **Type-03** | 指标观测 | ① 数据是**持续采集**的时序指标 ② 关注趋势 / 异常尖刺 / 水位 ③ **无终态**（持续观测，而非一次性完成）④ 需多维切片与联动 |
| **Type-04** | 依赖链路 | ① 对象之间存在调用或网络依赖关系 ② 目标是定位瓶颈 / 故障源 ③ 关注影响半径与扩散路径 |
| **Type-05** | 离散事件 | ① 有明确的异常信号源（告警 / 攻击）② 需聚合降噪 ③ 需按时序还原因果证据链 ④ 需跨角色协同处置闭环 |
| **Type-06** | 长期规则 | ① 是**长期生效**的判定逻辑 ② 含多分支条件与优先级 ③ 有配额 / 冲突 / 越界防护诉求 |
| **Type-07** | 执行作业 | ① 以「任务 / 作业」为对象 ② 有明确的开始与结束（run-to-completion）③ 有结果产物 ④ 需追溯多轮执行历史 |

### 2.3 易混淆类型成对辨析

> ⚠️ 目前**仅补齐了 Type-07 相关**的类型对（因目前只有该类型做过蒸馏）。其余类型对的辨析**待相应类型蒸馏后再补充**，在此之前请勿臆断。

| 易混类型对 | 辨析要点 |
| :--- | :--- |
| **Type-07 ↔ Type-01** | 主键是"作业"还是"实体"。**对象是名词，任务是动词** |
| **Type-07 ↔ Type-02** | Type-02 强调**双向打通**（控制流 + 数据流）；Type-07 是**单向**的"下发执行 ➔ 回收结果" |
| **Type-07 ↔ Type-03** | Type-03 是**持续观测**的时序指标；Type-07 是**离散有始有终**的作业快照 |
| **Type-07 ↔ Type-06** | Type-06 是**长期生效的判定逻辑**；Type-07 是**有执行历史**的执行计划 |

### 2.4 防误判警示

> ⚠️ **"有周期"不等于 Type-07**。周期只是任务的调度属性之一，判据始终是 2.2 表中的识别特征，而非"有没有定时"。
> ⚠️ **不要误判为 Type-07**：若只是"点一下立即执行、无周期无历史、无需任务清单"，那它是**轻量操作**，用弹窗向导即可，不应建成任务体系。

---

## 📚 三、各类型分册索引（定性后点击进入对应分册）

> 🚧 **重要状态声明（当前处于建设期，请勿视为完备经验底座）**：
> 本库**绝大多数分册尚未真正完成蒸馏**，现有内容普遍较浅、仅为雏形。
> **7 个类型中，目前仅 Type-07 完成了一次符合新标准 Schema 的完整蒸馏，其余均处于"未蒸馏"状态**。
> 在步骤 2 借鉴时，请务必结合分册内的实际内容深度判断，**不要把"文件存在"误读为"经验已就绪"**。
>
> ⚠️ **另需说明（2026-09-17 清理）**：原库中的「SASE 终端管理」「AES 资产管理」「数据集成管道与多源对接向导」三个案例，经追溯核查**无对应投喂物料、也无蒸馏提案与确认记录**，属建立本库时写入的**未经核实的示意性内容**，已在本次清理中全部移除并改为占位。**请勿将其当作真实经验引用。**

| 类型 | 分册文件 | 蒸馏状态 | 真实内容与缺口（如实描述） |
| :---: | :--- | :--- | :--- |
| **Type-01** | [对象与资产管理类](./标杆案例库/Type-01-对象与资产管理类.md) | ⚪ **未蒸馏** | 仅有 6 大通用设计场景清单；原「SASE / AES」两个**未经核实**的示意性案例已清空为占位 |
| **Type-02** | [配置对接与数据同步类](./标杆案例库/Type-02-配置对接与数据同步类.md) | ⚪ **未蒸馏** | 仅有 6 大通用设计场景清单；原「数据集成管道」**未经核实**的示意性案例已清空为占位 |
| **Type-03** | [监控度量与驾驶舱类](./标杆案例库/Type-03-监控度量与驾驶舱类.md) | ⚪ **未蒸馏（仅雏形）** | 只有 4 条通用设计场景清单，**无案例、无 Why、无 Token、无实例截图** |
| **Type-04** | [链路排障与拓扑分析类](./标杆案例库/Type-04-链路排障与拓扑分析类.md) | ⚪ **未蒸馏（仅雏形）** | 只有 4 条通用设计场景清单，缺口同 Type-03 |
| **Type-05** | [事件研判与调查处置类](./标杆案例库/Type-05-事件研判与调查处置类.md) | ⚪ **未蒸馏（仅雏形）** | 只有 3 条通用设计场景清单，缺口同 Type-03 |
| **Type-06** | [策略规则与编排类](./标杆案例库/Type-06-策略规则与编排类.md) | ⚪ **未蒸馏（仅雏形）** | 只有 3 条通用设计场景清单，缺口同 Type-03 |
| **Type-07** | [检测任务与批量巡检类](./标杆案例库/Type-07-检测任务与批量巡检类.md) | ✅ **已改造为新结构** ｜ 案例 1 / 经验 0 | **样板分册**：上半经验层（类型认知 + 6 大设计要素 + 经验库）+ 下半案例层（XDR 检测任务）。⚠️ 经验层需 **≥2 案例**方可归纳，当前仅 8 条"经验候选" |

> 📌 **待蒸馏清单 (Backlog)** —— 按需认领，认领后按 [`标杆方案案例蒸馏指南.md`](../../learning-materials/标杆方案案例蒸馏指南.md) 作业，产出写入对应分册并回填本表状态：
>
> | 序号 | 待办类型 | 需要的物料（形态） | 优先级 |
> | :---: | :--- | :--- | :---: |
> | 1 | **Type-01 首次蒸馏** | 对象与资产管理类的真实方案物料（设计稿截图 / 复盘文档 / 可访问链接任一） | 中 |
> | 2 | **Type-02 首次蒸馏** | 配置对接与数据同步类的真实方案物料 | 中 |
> | 3 | **Type-03 首次蒸馏** | 监控驾驶舱类的优秀方案（大盘 / 大屏 / 看板） | 低 |
> | 4 | **Type-04 首次蒸馏** | 链路排障与拓扑类的优秀方案（调用链 / 网络拓扑） | 低 |
> | 5 | **Type-05 首次蒸馏** | 事件研判类的优秀方案（告警中心 / 事件调查） | 低 |
> | 6 | **Type-06 首次蒸馏** | 策略编排类的优秀方案（WAF / 网关 / 零信任策略） | 低 |

> 🛠️ **分册维护约定**：
> 1. **一类型一文件**：新增蒸馏时**只读写对应分册** + 更新本页索引，严禁再把案例内容塞回本文档；
> 2. **两段式统一骨架（每个分册必须遵守）**：
>    > **上半部分 = 经验层（稳定资产 · 最终沉淀物）**
>    > `一、需求类型认知`（是什么 / 解决什么问题 / 产品形态谱系；**类型识别与边界判定统一见门户第二节，不在分册内重复**）
>    > `二、设计要素清单`（**表格化**：编号 ｜ 设计要素 ｜ 考量什么 ｜ 自检问句 ｜ 可选解法空间；该表同时充当「案例 × 要素」差异性对比的**行基准**）
>    > `三、设计经验库`（★ `3.1 设计经验` ｜ `3.2 待确认经验`；条目字段固定为 **编号 ｜ 设计经验项 ｜ 备注 ｜ 来源 ｜ 匹配案例**）
>    >
>    > **下半部分 = 案例层 + 应用层（证据与落地）**
>    > `四、案例导入蒸馏`（案例 A/B…，每个案例按设计要素逐项记录做法 + Why + Token）
>    > `五、本次设计要素选型工作表`（**空表模板**，填好的结果写入项目自己的步骤 2 产出）
>    >
>    > 完整样板见 [Type-07 分册](./标杆案例库/Type-07-检测任务与批量巡检类.md)。
> 3. **归纳流程（蒸馏的第三步，不可省略）**：
>    `读物料 → 写案例（第四节）→ **横向对比多案例 → 归纳经验（第三节）** → 回填`
>    - 新增案例后**必须回头更新第三节**，否则经验层会停留在旧结论；
>    - 经验**不做分类**，1~N 条即可（踩坑经验同样是经验，直接写成一条）；
>    - **来源**必须标注：`🤖 推理`（织雨从案例归纳，**需人工复核**）或 `👤 人工`（规划人员直接输入）；
>    - 织雨判断合理的经验**可直接沉淀进 3.1**；**证据不足或存疑的先写入 3.2 待确认**，不混入正式经验。
> 4. **可追溯契约（硬性）**：每条经验必须标注「匹配案例」与「来源」；每个案例必须标注「物料来源 + 蒸馏日期 + 案例独立性」。**无来源的内容一律不得写入分册。**
> 5. **新增需求类型**：若判定现有 7 类均不覆盖，需先在本页【全景图】新增类型（含业务本质与场景举例），再新建对应分册。

---

## 🎯 四、设计点输出规范模板 (Output Contract for Step 2)

在完成步骤 2 的标杆学习后，织雨必须按照以下结构化格式向规划人员输出《设计点思考与拟解决方案》：

```markdown
### 💡 步骤 2：设计点思考与标杆借鉴推演

1. **本次需求类型判定**：【属于 Type-0X: xxx 类需求】
2. **标杆案例映射与启发**：
   - 参照标杆：[如 XDR 检测任务（当前库内唯一已蒸馏案例）]
   - 借鉴点：[如复用其“表格+640px画像抽屉”架构与“原地连通性测试”机制]
   - 本次个性化差异：[说明本次业务与标杆不同的地方，如本次数据量较小但对审计日志要求更高]
3. **关键业务共性场景覆盖清单**：
   - [x] 场景 1（接入与连通性）：设计了 In-situ 原地检测机制；
   - [x] 场景 2（呈现与查筛）：采用 32px Pro-Search + 高密度表格；
   - [x] 场景 3（高危防呆）：退出删除设计了影响资产清单与口令验证。
4. **核心设计发力点 (L1 方向 ➔ L2 机制)**：
   - **发力点 1**：[方向] ➔ [对应机制/范式]
   - **发力点 2**：[方向] ➔ [对应机制/范式]
```
